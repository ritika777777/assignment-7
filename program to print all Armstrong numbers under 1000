#include<stdio.h>
#include<math.h>
int main()
{
    int n=1,a,b,c,sum,num;
    while(n<1000){
    a=n/100;
    b=(n%100)/10;
    c=n%10;
    sum=n;
    num=pow(a,3)+pow(b,3)+pow(c,3);
    if(sum==num){
    printf("\n %d",sum);
    }
   n++;
    }
    return 0;
}

